# Sentry Backend + Docker Environment Variables
# Copy this to .env.local and fill in your credentials

SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# URLs for SDK verification and DSN
SDK_VERIFICATION_BASE_URL=http://localhost:3000
SDK_DEFAULT_DSN=http://localhost:8001

# Validation mode (false for dev, true for prod)
SDK_SCHEMA_STRICT_STARTUP=false
